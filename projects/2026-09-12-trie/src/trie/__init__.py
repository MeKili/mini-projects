"""trie — a prefix tree for efficient string storage, search, and autocomplete.

Insert and search for strings in O(m) time, where m is the string length.
Supports prefix matching and retrieval of all words with a given prefix.
"""

from trie.core import Trie

__all__ = ["Trie"]
__version__ = "0.1.0"
